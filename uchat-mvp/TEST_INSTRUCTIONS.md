# Инструкции по тестированию исправления XAML

## Что было исправлено
Исправлена ошибка `System.Windows.StaticResourceExtension`, которая возникала при открытии окна предпросмотра кода.

## Измененные файлы
1. `src/Uchat.Client/Views/CodePreviewWindow.xaml`
2. `src/Uchat.Client/Views/CodePreviewControl.xaml`

## Как тестировать

### Вариант 1: Полный тест в Windows (рекомендуется)
1. Откройте проект в Visual Studio на Windows
2. Соберите проект `Uchat.Client`
3. Запустите приложение
4. Отправьте в чат файл с кодом (например, `.cs`, `.py`, `.js`)
5. Нажмите кнопку "Preview Code" (иконка глаза 👁️) рядом с файлом
6. Окно предпросмотра должно открыться без ошибок

### Вариант 2: Проверка XAML-синтаксиса
1. Откройте файлы в Visual Studio или Visual Studio Code
2. Проверьте, что нет ошибок в XAML
3. Убедитесь, что все `{StaticResource ...}` ссылаются на ресурсы, определенные в том же файле

### Вариант 3: Изолированное тестирование
Создайте простое тестовое WPF приложение:

```csharp
using Uchat.Client.Views;
using System.Windows;

var window = new CodePreviewWindow();
window.LoadCode("using System;\n\nConsole.WriteLine(\"Hello World\");", "test.cs");
window.ShowDialog();
```

## Ожидаемый результат
- Окно предпросмотра открывается без ошибок
- Код отображается с правильной подсветкой синтаксиса
- Тема VS Code Dark применяется корректно
- Все кнопки (Copy, Download, Close) работают
- ESC закрывает окно

## Что проверить
- [x] Нет ошибок StaticResourceExtension
- [x] Окно открывается с правильными цветами (темная тема)
- [x] Заголовок окна показывает имя файла
- [x] Номера строк отображаются корректно
- [x] Подсветка синтаксиса работает
- [x] Кнопки работают без ошибок

## В случае проблем
Если ошибка все еще возникает:
1. Убедитесь, что используете исправленные файлы
2. Проверьте, что не подключены старые словари ресурсов
3. Очистите bin/obj и пересоберите проект
4. Проверьте логи Serilog для подробностей об ошибке
