<UserControl x:Class="Uchat.Client.Views.WelcomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">

    <UserControl.Resources>
        <SolidColorBrush x:Key="BgDarkBrush" Color="#111118"/>
        <SolidColorBrush x:Key="BlobPurpleBrush" Color="#422F91"/>
        <SolidColorBrush x:Key="TextWhiteBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextBeigeBrush" Color="#DBCFB6"/> <SolidColorBrush x:Key="TextGreyBrush" Color="#8F8F9E"/>
        <SolidColorBrush x:Key="ButtonBorderBrush" Color="#3A3A45"/>
        <SolidColorBrush x:Key="ButtonHoverBrush" Color="#2A2A35"/>

        <Style x:Key="GlassButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextGreyBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="0,12"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{StaticResource ButtonBorderBrush}"
                                BorderThickness="1"
                                CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource ButtonHoverBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#555566"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="{StaticResource BgDarkBrush}">
        
        <Ellipse Width="400" Height="400" 
                 HorizontalAlignment="Left" VerticalAlignment="Top"
                 Margin="-100,-100,0,0" 
                 Fill="{StaticResource BlobPurpleBrush}" Opacity="0.6">
            <Ellipse.Effect>
                <BlurEffect Radius="120"/>
            </Ellipse.Effect>
        </Ellipse>

        <Ellipse Width="500" Height="500" 
                 HorizontalAlignment="Right" VerticalAlignment="Center"
                 Margin="0,0,-150,0" 
                 Fill="{StaticResource BlobPurpleBrush}" Opacity="0.5">
            <Ellipse.Effect>
                <BlurEffect Radius="140"/>
            </Ellipse.Effect>
        </Ellipse>

        <Ellipse Width="300" Height="300" 
                 HorizontalAlignment="Center" VerticalAlignment="Bottom"
                 Margin="0,0,0,-100" 
                 Fill="{StaticResource BlobPurpleBrush}" Opacity="0.4">
            <Ellipse.Effect>
                <BlurEffect Radius="100"/>
            </Ellipse.Effect>
        </Ellipse>

        <StackPanel HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    MaxWidth="500">

            <TextBlock TextAlignment="Center" 
                       FontSize="42" 
                       FontWeight="Bold"
                       Margin="0,0,0,15">
                <Run Text="Welcome to " Foreground="{StaticResource TextWhiteBrush}"/>
                <Run Text="Uchat" Foreground="{StaticResource TextBeigeBrush}"/>
                <Run Text="👋" FontFamily="Segoe UI Emoji"/>
            </TextBlock>

            <TextBlock Text="Uchat is the instant messenger built for developers, featuring seamless code sharing, syntax highlighting, and powerful workflow integrations."
                       FontSize="14"
                       Foreground="{StaticResource TextGreyBrush}"
                       TextAlignment="Center"
                       TextWrapping="Wrap"
                       LineHeight="20"
                       Margin="0,0,0,50"/>

            <Button Content="Log in"
                    Command="{Binding NavigateToLoginCommand}"
                    Style="{StaticResource GlassButtonStyle}"
                    Width="220"/>

            <Button Content="Register"
                    Command="{Binding NavigateToRegisterCommand}"
                    Style="{StaticResource GlassButtonStyle}"
                    Width="220"/>

        </StackPanel>
    </Grid>
</UserControl>