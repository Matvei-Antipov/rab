<UserControl x:Class="Uchat.Client.Views.GeneratorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d"
             d:DesignHeight="750" d:DesignWidth="1000">

    <UserControl.Resources>
        <Style x:Key="GlassPanelBorderStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="18"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="{DynamicResource GlassPanelBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource GlassPanelBorderBrush}"/>
        </Style>

        <Style TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBoxItem}">
                        <Border x:Name="Bd" CornerRadius="10" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#25FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#35FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="RoundNavButtonStyle">
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Margin" Value="0,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="{DynamicResource NavButtonBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource IconBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" CornerRadius="20"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <Border.Effect>
                                <DropShadowEffect Color="#000000" Direction="315" ShadowDepth="2" BlurRadius="4" Opacity="0.3"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource NavButtonHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource NavButtonPressedBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PromptTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextTertiaryBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="AcceptsReturn" Value="True"/>
            <Setter Property="CaretBrush" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <ScrollViewer x:Name="PART_ContentHost"/>
                            
                            <TextBlock x:Name="PlaceholderText"
                                       Text="{TemplateBinding Tag}"
                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                       FontStyle="Italic"
                                       IsHitTestVisible="False"
                                       Margin="2,0,0,0"
                                       Visibility="Collapsed"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="{DynamicResource AppBackgroundImageSource}" Stretch="UniformToFill"/>
        </Grid.Background>
        
        <Border Background="{DynamicResource AppOverlayBrush}"/>

        <Grid x:Name="MainContentGrid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Margin="10" Background="Transparent">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,10">
                    <Border Style="{StaticResource GlassPanelBorderStyle}"/>

                    <ListBox Margin="0,10,0,10"
                             Background="Transparent"
                             BorderThickness="0"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             ScrollViewer.VerticalScrollBarVisibility="Hidden"
                             HorizontalAlignment="Center"
                             HorizontalContentAlignment="Center">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Button Content="" Style="{StaticResource RoundNavButtonStyle}"/>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                        
                        <ListBox.Items>
                            <sys:String>1</sys:String>
                            <sys:String>2</sys:String>
                            <sys:String>3</sys:String>
                        </ListBox.Items>
                    </ListBox>
                </Grid>

                <Grid Grid.Row="1" Margin="0,0,0,0">
                    <Border Style="{StaticResource GlassPanelBorderStyle}" CornerRadius="25"/>

                    <StackPanel VerticalAlignment="Center" Margin="0,15,0,15" HorizontalAlignment="Center">
                        
                        <Button Style="{StaticResource RoundNavButtonStyle}" Background="#6E7195" ToolTip="Gallery">
                            <TextBlock Text="&#xE91B;" FontFamily="Segoe MDL2 Assets" FontSize="18" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Button>
                        
                        <Button Command="{Binding DataContext.ToggleThemeCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                Style="{StaticResource RoundNavButtonStyle}" 
                                Background="#8C7B30" 
                                ToolTip="Theme">
                            <TextBlock Text="{Binding DataContext.ThemeIcon, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=&#xE708;}" FontFamily="Segoe MDL2 Assets" FontSize="18" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Button>
                        
                        <Button Command="{Binding NavigateToSettings}"
                                Style="{StaticResource RoundNavButtonStyle}" 
                                Background="#4E5165" 
                                ToolTip="Settings">
                            <TextBlock Text="&#xE713;" FontFamily="Segoe MDL2 Assets" FontSize="18" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Grid>

            <Grid Grid.Column="1" Margin="20,10,20,10">
                <Border Style="{StaticResource GlassPanelBorderStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="60"/>
                            <RowDefinition Height="30"/>
                            <RowDefinition Height="200"/>
                            <RowDefinition Height="50"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="50"/>
                            <RowDefinition Height="40"/>
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Button Grid.Column="0" 
                                    Command="{Binding NavigateBackCommand}" 
                                    Style="{StaticResource RoundNavButtonStyle}" 
                                    Width="36" Height="36" 
                                    HorizontalAlignment="Left"
                                    Margin="10,0,0,0" 
                                    ToolTip="Back">
                                <TextBlock Text="&#xE72B;" FontFamily="Segoe MDL2 Assets" FontSize="16"/>
                            </Button>
                            
                            <TextBlock Grid.Column="1" Text="TARAS 1.0" FontSize="16" Foreground="{DynamicResource TextPrimaryBrush}" 
                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                        <Border Grid.Row="0" VerticalAlignment="Bottom" Height="1" Background="{DynamicResource SeparatorBrush}"/>

                        <Grid Grid.Row="1" Margin="25,0">
                            <TextBlock Text="Build: YYYY-MM-DD" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                            <TextBlock Text="Version: 1.0.0-beta.1" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            <TextBlock Text="Privacy &amp; Security Terms" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11" HorizontalAlignment="Right" VerticalAlignment="Center" Cursor="Hand"/>
                        </Grid>
                        <Border Grid.Row="1" VerticalAlignment="Bottom" Height="1" Background="{DynamicResource SeparatorBrush}"/>

                        <Grid Grid.Row="2" Margin="25,15">
                            <TextBox Style="{StaticResource PromptTextBoxStyle}" 
                                     Tag="A modern office desk with laptop and cup of coffee, minimalist..." 
                                     VerticalAlignment="Top" Height="150"/>
                            
                            <TextBlock Text="45/500 characters" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" 
                                       HorizontalAlignment="Right" VerticalAlignment="Bottom"/>
                        </Grid>
                        <Border Grid.Row="2" VerticalAlignment="Bottom" Height="1" Background="{DynamicResource SeparatorBrush}"/>

                        <Grid Grid.Row="3">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="1"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Button Grid.Column="0" Content="SAVE INTO" Background="Transparent" Foreground="{DynamicResource TextPrimaryBrush}"
                                    BorderThickness="0" Cursor="Hand" FontSize="12">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="Transparent">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Foreground" Value="{DynamicResource AccentColorBrush}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>

                            <Border Grid.Column="1" Background="{DynamicResource SeparatorBrush}" Margin="0,10"/>

                            <Button Grid.Column="2" Content="SEND TO" Background="Transparent" Foreground="{DynamicResource TextPrimaryBrush}"
                                    BorderThickness="0" Cursor="Hand" FontSize="12">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="Transparent">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Foreground" Value="{DynamicResource AccentColorBrush}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </Grid>
                        <Border Grid.Row="3" VerticalAlignment="Bottom" Height="1" Background="{DynamicResource SeparatorBrush}"/>

                        <Grid Grid.Row="4" Margin="25,15">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <TextBlock Grid.Row="0" Text="[TARAS V1] Generated X min ago... :" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="12" Margin="0,0,0,10"/>
                            
                            <Border Grid.Row="1" Width="180" Height="100" CornerRadius="4" HorizontalAlignment="Left" Background="{DynamicResource NavButtonBackgroundBrush}">
                                <Grid>
                                    <TextBlock Text="TARAS" FontWeight="Bold" FontSize="28" Foreground="#503020" 
                                               HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.9">
                                        <TextBlock.Effect>
                                            <DropShadowEffect Color="#FFFFFF" BlurRadius="2" ShadowDepth="0"/>
                                        </TextBlock.Effect>
                                    </TextBlock>

                                    <Border Background="#90202020" CornerRadius="4" Width="30" Height="20" 
                                            HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="5">
                                         <TextBlock Text="&#xE74D;" FontFamily="Segoe MDL2 Assets" FontSize="10" Foreground="{DynamicResource TextInverseBrush}" 
                                                    HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </Grid>
                            </Border>
                        </Grid>
                        
                        <Border Grid.Row="5" VerticalAlignment="Top" Height="1" Background="{DynamicResource SeparatorBrush}"/>

                        <Grid Grid.Row="5" Margin="10,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="50"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBlock Text="&#xE81C;" FontFamily="Segoe MDL2 Assets" FontSize="20" Foreground="{DynamicResource IconBrush}" 
                                       VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            
                            <Border Grid.Column="0" HorizontalAlignment="Right" Width="1" Background="{DynamicResource SeparatorBrush}" Margin="0,10"/>

                            <TextBlock Grid.Column="1" Text="[Згенеруй мені банан], [Generate Galaxy Z Fold for my granny], [Millenial...]" 
                                       Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" VerticalAlignment="Center" Margin="15,0,0,0"
                                       TextTrimming="CharacterEllipsis"/>
                        </Grid>
                        
                        <Border Grid.Row="6" VerticalAlignment="Top" Height="1" Background="{DynamicResource SeparatorBrush}"/>

                        <Button Grid.Row="6" Content="GENERATE" Background="Transparent" Foreground="{DynamicResource TextTertiaryBrush}"
                                BorderThickness="0" Cursor="Hand" FontSize="12" FontWeight="SemiBold">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="Transparent">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</UserControl>