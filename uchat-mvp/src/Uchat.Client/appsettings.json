{
  "ServerConfiguration": {
    "BaseUrl": "CONFIGURED_FROM_CLI_ARGUMENTS",
    "WebSocketUrl": "CONFIGURED_FROM_CLI_ARGUMENTS"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning"
    }
  }
}
