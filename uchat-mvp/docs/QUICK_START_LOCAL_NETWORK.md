# Быстрый старт - Локальная сеть

## ⚠️ Важно про .env файлы

**`.env` файл НЕ нужен для локальной сети!**

- На **ПК1 (Сервере)**: все настройки в `appsettings.json`
- На **ПК2 (Клиенте)**: `.env` вообще не используется - IP и порт передаются через аргументы командной строки

---

## 🚀 Быстрая настройка за 5 минут

### ПК1 (Сервер) - Что нужно сделать:

1. **Установите БД** (если еще не установлены):
   - Oracle XE
   - MongoDB
   - Redis (или Memurai для Windows)

2. **Найдите IP-адрес ПК1**:
   ```powershell
   ipconfig
   # Запишите IPv4 адрес (например: 192.168.1.100)
   ```

3. **Настройте CORS** в `uchat-mvp\src\Uchat.Server\appsettings.json`:
   ```json
   "Cors": {
     "AllowedOrigins": [ "*" ],
     "AllowCredentials": false
   }
   ```
   ⚠️ Используйте `"*"` только для тестирования!

4. **Откройте порт в брандмауэре**:
   ```powershell
   New-NetFirewallRule -DisplayName "Uchat Server" -Direction Inbound -LocalPort 5251 -Protocol TCP -Action Allow
   ```

5. **Запустите сервер**:
   ```powershell
   cd uchat-mvp\src\Uchat.Server
   dotnet run 5251
   ```

---

### ПК2 (Клиент) - Что нужно сделать:

1. **Установите .NET 8 Runtime** (если еще не установлен)

2. **Запустите клиент**:
   ```powershell
   cd uchat-mvp\src\Uchat.Client
   dotnet run 192.168.1.100 5251
   ```
   Замените `192.168.1.100` на IP ПК1!

---

## ✅ Проверка

### На ПК2 проверьте доступность сервера:
```powershell
Test-NetConnection -ComputerName 192.168.1.100 -Port 5251
```

Или откройте в браузере:
```
http://192.168.1.100:5251/health
```

Должен вернуться: `{"Status":"Healthy","Timestamp":"..."}`

---

## 🔧 Если не работает

1. **Проверьте брандмауэр** на ПК1
2. **Проверьте, что оба ПК в одной сети**
3. **Проверьте IP-адрес** сервера: `ipconfig` на ПК1
4. **Проверьте логи** сервера в консоли
5. **Проверьте CORS** настройки в `appsettings.json`

---

## 📝 Команды для копирования

### ПК1 (Сервер):
```powershell
# Найти IP
ipconfig

# Открыть порт
New-NetFirewallRule -DisplayName "Uchat Server" -Direction Inbound -LocalPort 5251 -Protocol TCP -Action Allow

# Запустить сервер
cd uchat-mvp\src\Uchat.Server
dotnet run 5251
```

### ПК2 (Клиент):
```powershell
# Запустить клиент (замените IP на IP ПК1!)
cd uchat-mvp\src\Uchat.Client
dotnet run 192.168.1.100 5251
```

---

## 🎯 Типичные проблемы

| Проблема | Решение |
|----------|---------|
| Клиент не подключается | Проверьте IP и порт, брандмауэр, CORS |
| Ошибка CORS | Добавьте IP клиента в `AllowedOrigins` или используйте `"*"` |
| Порт занят | Используйте другой порт (8080, 9000) |
| БД недоступна | Проверьте, что Oracle/MongoDB/Redis запущены |

---

📖 **Подробная инструкция**: см. `LOCAL_NETWORK_SETUP.md`

