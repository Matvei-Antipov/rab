# Руководство: Функция "Умный предпросмотр кода"

## 📝 Описание

Функция "Умный предпросмотр кода" позволяет просматривать файлы с исходным кодом прямо в мессенджере, без необходимости скачивания и открытия во внешних редакторах. Система автоматически определяет языки программирования и отображает код с подсветкой синтаксиса в стиле Visual Studio Code (тёмная тема).

## ✨ Возможности

### Автоматическое определение языка
Система распознаёт файлы по расширению:
- **C#**: `.cs`
- **Python**: `.py`
- **JavaScript/TypeScript**: `.js`, `.ts`, `.jsx`, `.tsx`
- **Java**: `.java`
- **C/C++**: `.cpp`, `.c`, `.h`, `.hpp`
- **Go**: `.go`
- **Rust**: `.rs`
- **PHP**: `.php`
- **Ruby**: `.rb`
- **Swift**: `.swift`
- **Kotlin**: `.kt`
- **HTML/CSS**: `.html`, `.css`, `.scss`
- **JSON/XML**: `.json`, `.xml`
- **SQL**: `.sql`
- **И многие другие...**

### Профессиональный интерфейс
- 🎨 **Тёмная тема VS Code** - комфортное чтение кода
- 🔢 **Номера строк** - удобная навигация
- 🌈 **Подсветка синтаксиса** - цветовое выделение элементов кода
- 📊 **Информация о файле** - название, язык, количество строк, размер

### Действия с кодом
- 📋 **Копировать** - скопировать весь код в буфер обмена
- 💾 **Скачать** - сохранить файл на диск
- ❌ **Закрыть** - закрыть окно предпросмотра

## 🚀 Как использовать

### 1. Отправка файла с кодом
1. Откройте чат
2. Нажмите кнопку вложения (скрепка)
3. Выберите файл с кодом (например, `script.py`)
4. Отправьте сообщение

### 2. Просмотр кода
1. Найдите вложение с файлом в сообщении
2. Обратите внимание на **синюю иконку глаза** 👁️ рядом с файлом
3. Нажмите на иконку глаза
4. Откроется окно предпросмотра кода

### 3. Работа с окном предпросмотра

#### Заголовок окна
```
[Иконка] script.py          Python • 67 строк • 2.5 KB
```

#### Просмотр кода
- Код отображается с подсветкой синтаксиса
- Слева видны номера строк
- Можно прокручивать по вертикали и горизонтали
- Шрифт: моноширинный (Consolas)

#### Кнопки действий
- **📋 Копировать код** - копирует весь код в буфер обмена
- **💾 Скачать** - открывает диалог сохранения файла
- **❌ Закрыть** - закрывает окно предпросмотра

## 🎨 Цветовая схема (VS Code Dark)

### Основные цвета
- **Фон**: Тёмно-серый (#1E1E1E)
- **Текст**: Светло-серый (#D4D4D4)
- **Номера строк**: Серый (#858585)

### Подсветка синтаксиса
- **Комментарии**: Зелёный (#6A9955)
  ```python
  # Это комментарий
  ```

- **Строки**: Оранжевый/коричневатый (#CE9178)
  ```python
  "Hello, World!"
  ```

- **Числа**: Светло-зелёный (#B5CEA8)
  ```python
  42, 3.14
  ```

- **Ключевые слова**: Фиолетовый/розовый (#C586C0)
  ```python
  import, def, class, if, else, return
  ```

- **Функции/методы**: Жёлтый (#DCDCAA)
  ```python
  def my_function():
  ```

- **Типы данных**: Голубой/бирюзовый (#4EC9B0)
  ```python
  str, int, float
  ```

## 💡 Примеры использования

### Пример 1: Python
```python
import os
from typing import List

def process_files(directory: str) -> List[str]:
    """Process all files in directory."""
    files = []
    for filename in os.listdir(directory):
        if filename.endswith('.txt'):
            files.append(filename)
    return files
```

**Отображение:**
- `import`, `from`, `def`, `return` - фиолетовые
- `"Process all files in directory."` - оранжевая
- `str`, `List` - голубые
- `process_files` - жёлтая

### Пример 2: C#
```csharp
using System;

public class Calculator
{
    public int Add(int a, int b)
    {
        return a + b;
    }
}
```

**Отображение:**
- `using`, `public`, `class`, `return` - фиолетовые
- `int` - голубой
- `Calculator`, `Add` - жёлтые
- Числа и операторы - соответствующие цвета

### Пример 3: JavaScript
```javascript
const users = [
    { name: "Alice", age: 30 },
    { name: "Bob", age: 25 }
];

function getNames(users) {
    return users.map(u => u.name);
}
```

**Отображение:**
- `const`, `function`, `return` - фиолетовые
- Строки `"Alice"`, `"Bob"` - оранжевые
- Числа `30`, `25` - светло-зелёные
- `getNames` - жёлтая

## ⚠️ Важно знать

### Когда появляется кнопка предпросмотра?
✅ **Появляется** для:
- Всех файлов с расширениями кода (.py, .cs, .js, и т.д.)
- Текстовых конфигурационных файлов (.json, .xml, .yaml)
- Скриптов (.sh, .bat, .ps1)

❌ **НЕ появляется** для:
- Изображений (.jpg, .png, .gif)
- Документов (.pdf, .docx)
- Архивов (.zip, .rar)
- Аудио/видео (.mp3, .mp4)

### Размер файла
- Система пытается отобразить любой размер файла
- Очень большие файлы (>1MB) могут загружаться медленно
- Рекомендуемый размер: до 500 KB

### Поддержка языков
- Для популярных языков (C#, Python, Java, JS) - полная подсветка
- Для редких языков - базовое отображение с номерами строк
- Неизвестные расширения отображаются как обычный текст

## 🔧 Технические детали

### Компоненты
- **AvalonEdit** - мощный редактор текста с подсветкой
- **CodeLanguageDetector** - определение языка по расширению
- **CodePreviewControl** - контрол для отображения кода
- **CodePreviewWindow** - модальное окно предпросмотра

### Горячие клавиши
- **Escape** - закрыть окно предпросмотра
- **Ctrl+C** - копировать выделенный текст
- **Ctrl+A** - выделить весь код

### Особенности
- Код доступен только для чтения (нельзя редактировать)
- Поддержка прокрутки колёсиком мыши
- Автоматическое определение кодировки
- Окно можно изменять по размеру

## 📚 Дополнительные ресурсы

- **CODE_PREVIEW_FEATURE.md** - подробная техническая документация (English)
- **TESTING_CODE_PREVIEW.md** - инструкции по тестированию (English)
- **test_code_example.py** - пример Python кода для тестирования
- **test_code_example.cs** - пример C# кода для тестирования
- **test_code_example.js** - пример JavaScript кода для тестирования

## 🎯 Преимущества

1. **Быстрый просмотр** - не нужно скачивать и открывать файл
2. **Профессиональный вид** - привычный интерфейс VS Code
3. **Удобство** - всё в одном окне мессенджера
4. **Безопасность** - просмотр без выполнения кода
5. **Совместная работа** - легко обсуждать код в чате

## 🐛 Решение проблем

### Проблема: Кнопка предпросмотра не появляется
**Причина**: Расширение файла не распознано как код  
**Решение**: Проверьте, что расширение в списке поддерживаемых

### Проблема: Нет подсветки синтаксиса
**Причина**: Язык не поддерживается AvalonEdit  
**Решение**: Код отобразится как обычный текст с номерами строк

### Проблема: Медленная загрузка
**Причина**: Файл слишком большой  
**Решение**: Для больших файлов используйте скачивание и локальный редактор

### Проблема: Неправильная кодировка (иероглифы)
**Причина**: Файл в неподдерживаемой кодировке  
**Решение**: Скачайте файл и откройте в редакторе с правильной кодировкой

## ✨ Будущие улучшения

Планируемые возможности:
- 🔍 Поиск внутри кода
- 📏 Изменение размера шрифта
- 🌓 Переключение светлой/тёмной темы
- 🖨️ Печать кода
- 📄 Экспорт в PDF
- 🔄 Сравнение версий кода
- ✏️ Редактирование с сохранением обратно в чат

---

**Наслаждайтесь удобным просмотром кода в Uchat! 🚀**
